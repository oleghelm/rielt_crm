{% extends 'desk.html.twig' %}

{% block body %}
    <div class="container-fluid">
        {{ form_start(form, {'action': path('crm_tools_lastActive')}) }}
        <div class="row">
            <div class="col-md-3 col-sm-6">{{ form_row(form.lastUpdateStart) }}</div>
            <div class="col-md-3 col-sm-6">{{ form_row(form.lastUpdateEnd) }}</div>
            <div class="col-md-2 col-sm-6">{{ form_row(form.entity) }}</div>
            <div class="col-md-2 col-sm-6">{{ form_row(form.user) }}</div>
            <div class="col-md-2 col-sm-6">
                <br>
                <button type="submit" formnovalidate class="btn btn-success"><i class="fa fa-search"></i></button>
                <a href="{{ path('crm_tools_lastActive')}}" class="btn btn-success"><i class="fa fa-times"></i></a>
                <br>
            </div>
        </div>
        {{ form_end(form) }}
        {% if(items) %}
            {% if(form.vars.data.entity == 'bid') %}
                {{ include('crm/bid/_list_lastActive.html.twig') }}
            {% elseif(form.vars.data.entity == 'object') %}
                {{ include('crm/object/_list_lastActive.html.twig') }}
            {% endif %}
        {% endif %}
        
{#        {{dump(form)}}#}
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{#    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.7.1/css/bootstrap-datepicker.min.css" />#}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {#<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.7.1/js/bootstrap-datepicker.min.js"></script>
    <script type="text/javascript">
        jQuery(document).ready(function(){
            $('.js-datepicker').datepicker({
                format: 'yyyy-mm-dd',
                language: 'uk'
            });
        })
    </script>#}
{% endblock %}